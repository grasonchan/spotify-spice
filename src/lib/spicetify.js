export const {
  classnames,
  URI,
  GraphQL,
  Config: cliConfig,
  Platform,
  Player,
  ContextMenu,
  showNotification,
  SVGIcons,
} = Spicetify;

export const { origin: originPlayer } = Player;

export const queueGetter = () => Spicetify.Queue;
