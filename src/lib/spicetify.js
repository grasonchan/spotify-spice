export const {
  classnames,
  URI,
  GraphQL,
  Config: cliConfig,
  Platform,
  Player,
  Menu: ProfileMenu,
  ContextMenu,
  showNotification,
  SVGIcons,
} = Spicetify;

export const { origin: originPlayer } = Player;

export const queueGetter = () => Spicetify.Queue;
